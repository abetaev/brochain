import type { JSX } from "solid-js";

// 🚧 Under construction. Brochain targets the vertical handheld layout for now;
// this is the seat the wider layout will take once that work starts.
export function Desktop(_props: { children: JSX.Element }): JSX.Element {
  throw new Error("🚧 The desktop layout is not built yet — use Handheld.");
}
